name = "pdfseal"
compatibility_date = "2024-09-23"
# SPA fallback: serve index.html for all unknown paths so deep links like
# /compress-pdf never 404 (required for SEO landing pages on Workers static assets)
assets = { directory = "./dist", not_found_handling = "single-page-application" }

[build]
command = "npm run build"
